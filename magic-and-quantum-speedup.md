# Magic(비안정자성)과 양자 속도우위 — 쇼어에서의 열린 문제

*연구 과제 노트. shor 레포의 상태벡터 시뮬레이션과 직접 연결.*

---

## 0. 한 줄

> 쇼어가 magic(비-Clifford 자원)을 *필요로 한다*는 것은 확정됐다. 그러나 **magic의
> *양*이 속도우위와 정량적으로 어떻게 연결되는지**, 특히 **QFT가 그 magic에서 하는
> 역할**은 열린 문제다. 이 레포의 시뮬레이션이 그 검증의 실험대가 된다.

---

## 1. 확정된 것 — magic의 필요성

- **Gottesman–Knill 정리:** Clifford(안정자) 회로 = 고전 컴퓨터로 효율적 시뮬레이션 가능.
- 따라서 쇼어의 **지수적 속도우위 ⟹ 비-Clifford = magic이 반드시 들어있다.**
- 쇼어에서 magic의 출처:
  - QFT의 작은각 제어위상 게이트 $R_k$ (각도 $\pi/2^k$). $k{=}2$는 $S$(Clifford),
    $k\geq3$($\pi/4{=}T$ 이하)는 **비-Clifford = magic**.
  - 모듈러 거듭제곱의 **Toffoli(CCNOT)** = 비-Clifford.
  - 중간 상태 $\sum_x|x\rangle|a^x\bmod N\rangle$ 자체가 비안정자(비선형 함수).

---

## 2. 열린 것 — 정량 관계 (연구 가치)

1. magic의 *양*(stabilizer Rényi 엔트로피 $M_2$, robustness of magic 등)이 회로 진행에
   따라 어떻게 변하나?
2. 그 양이 속도우위·문제크기·성공확률과 **정량적으로** 어떻게 연결되나?
   (필요성은 확실, *정량 법칙*은 학계도 미정.)
3. **QFT의 역할:** QFT는 magic을 *생성*하나, *재분배*하나, *구조화*하나?

---

## 3. 이 레포에서의 구체화 (중요한 단서)

`shor.py`의 `simulate_period_finding`은 두 지름길을 쓴다:
1. **작업 레지스터를 먼저 측정** → 계산 레지스터가 **주기적 빗(comb) 상태**로 붕괴:
   $$|\psi\rangle=\frac{1}{\sqrt{m}}\sum_{j=0}^{m-1}|x_0+jr\rangle \qquad (r=\text{주기})$$
2. **역 QFT를 `np.fft.fft`로 통째 적용** (게이트 분해 없음).

→ **함의:** 게이트 차원의 magic($R_k$, Toffoli)이 FFT 블랙박스로 **"컴파일되어 사라진다".**
그래서 이 레포에서 magic을 연구하려면 둘 중 하나의 길을 택해야 한다:
- **(a) comb 상태의 magic을 직접 측정** — `amps` 벡터가 이미 있으니 즉시 계산 가능.
- **(b) QFT를 $R_k$ 게이트로 분해**해 비-Clifford(T-count)를 센다.

---

## 4. 연구 과제

### T1 — comb 상태의 magic (가장 가까운 입구)
주기 $r$, 크기 $Q=2^t$의 함수로 $|\psi\rangle=\frac1{\sqrt m}\sum_j|x_0+jr\rangle$의
stabilizer Rényi 엔트로피 $M_2$를 측정.
> **가설:** $r$이 **2의 거듭제곱**이면 comb 상태는 (하위 $s$비트 고정 ⊗ 상위비트 $|+\rangle$)
> = **안정자 상태 → $M_2=0$**. $r$에 **홀수 인수**가 있으면 텐서구조에 안 맞아 → $M_2>0$.
> → "인수분해를 *비자명하게* 만드는 주기"와 magic이 같이 켜지는지 검증.

이게 사실이면, magic이 단순한 부산물이 아니라 **문제의 난이도(주기 구조)와 묶여 있다**는
첫 증거. (검증/반증 모두 명확.)

### T2 — QFT의 T-count
$t$큐비트 QFT를 제어-$R_k$로 분해하고 비-Clifford 게이트 수를 $n$의 함수로 센다.
알려진 쇼어 T-복잡도와 대조 → "QFT가 얼마만큼의 magic을 *공급*하는가" 정량화.

### T3 — 대조군 (Simon vs Shor)
레포에 `experiments/simon_sigma_curve.py`가 있다. Simon 알고리즘(주로 Hadamard+오라클,
선형구조)과 쇼어의 magic 궤적을 비교 → "선형(Clifford스러운) 문제 vs 비선형(magic 필요)
문제"의 magic 차이를 직접 본다.

### T4 — 속도우위 연결
최대/총 magic을 문제크기 $N$·주기구조·성공확률 $P(\text{peak})$과 회귀 →
정량 관계의 첫 경험식.

---

## 5. 측정 도구 (즉시 사용 가능)

순수상태 $|\psi\rangle$ ($n$큐비트)의 stabilizer 2-Rényi 엔트로피:
$$M_2 = -\log_2\!\Big(\frac1{2^n}\sum_{P\in\mathcal P_n}\langle\psi|P|\psi\rangle^4\Big),
\qquad M_2(\text{안정자})=0.$$
- comb 상태는 이미 `amps`(길이 $Q$)로 존재 → 큐비트 수 $t=\log_2 Q$에 대해 직접 계산.
- 전체 Pauli 합은 $4^t$개 → $t\lesssim10$($Q\lesssim1024$)까지 정확 계산 가능.
  더 큰 경우 Pauli 샘플링/MPS 근사(arXiv:2501.12146 류).

---

## 6. 정직한 경계

- magic은 속도우위에 **필요**하나 단독으로 **충분**하진 않다(얽힘과 함께 작동).
- 이 레포의 지름길(작업레지스터 선측정 + FFT)은 **전체 유니터리가 아니라 한계 분포**를
  재현한다. 따라서 "회로가 *소비*하는 magic"과 "comb 상태가 *품은* magic"은 다른 양 —
  구분해서 해석해야 한다.
- magic↔속도우위의 **정량 법칙은 학계에서도 미정** — 그래서 연구 가치가 있다.

---

## 7. 배경 — 왜 이 질문이 생겼나

분리된 한 작업(창발 시공간/홀로그래픽 코드, 별도 레포로 이관)에서, **비국소 magic이
홀로그래픽 코드의 넓이연산자를 상태의존으로 만든다**(중력 역반응; Cao et al.
arXiv:2306.14996)는 효과를 수치로 재현했다. 그 과정에서 **magic이 (i) 쇼어의 양자
속도우위와 (ii) 시공간 기하 둘 다의 공통 자원**임이 드러났다 — 같은 비안정자성 자원의
두 쓰임. 그 통찰이 "그렇다면 쇼어에서 magic의 *양*은 속도우위와 어떻게 연결되나"라는
이 레포 고유의 질문으로 이어졌다.
