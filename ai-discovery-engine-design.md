# JAMES-DISCOVER — 수학적 직관 코딩 / 알고리즘 발견 AI 시스템 설계·타당성 검토

*"지금까지의 연구를 바탕으로, 새로운 수학적 정리·알고리즘 최적화를 자동으로 찾아내는 AI 시스템을
만들 수 있는가"에 대한 설계·타당성 검토. 코드 없이 아키텍처·실현가능성·위험·로드맵만 다룬다.*

*기준일 2026-06-19. 전제 문서: `magic-program-overview.md`(통합본), `magic-results.md`(명제),
`HISTORY.md`(연대기), `frontier.md`(틀 밖 탐색). 본 문서는 그 위에 "발견 자동화" 층을 얹는 검토.*

---

## 0. 한 줄 결론

> **이 레포는 이미 "사람이 손으로 돌리는 발견 루프"를 갖고 있다.** 측정 인프라(`magic.py`)·
> 닫힌형 발견(Sidon 값, 가법에너지)·반례 기반 자기수정(누적 9+건 철회)이 그것이다. "AI 시스템"은
> 새 패러다임이 아니라 **이 루프의 4단계(생성·측정·추측·반증)를 기계화**하는 것이고, 이 도메인은
> *정확한 측정 + 작은 상태공간 + 닫힌형이 실제로 존재*한다는 세 조건 덕에 자동 발견에 **유난히 잘 맞는다.**
> 권장: **좁은 PoC(이미 손으로 찾은 닫힌형을 AI가 *재발견*하는지)부터** 시작하고, 재발견에 성공하면
> 미탐 영역(Δ(M,N) 보정항, 비-flat Grover 닫힌형)으로 확장한다.

---

## 1. 동기 — 이 프로젝트의 연구 방식은 이미 "발견 루프"다

`HISTORY.md`가 명시하는 메타-특징은 **claim → 검증 → 자기수정(retraction)의 반복**이다. 이를 추상화하면:

```
 [생성]  상태/구조를 만든다        예: 마커집합 W, 오라클 f, 잡음 σ
   │
 [측정]  불변량을 정확히 잰다       예: M₂=sre2(ψ), 자기상관 A_W, τ, 가법에너지 E
   │
 [추측]  관계식을 손으로 적는다     예: M₂ = log₂(M³/(7M−6)), p(σ)=ρ+(p₀−ρ)e^{−σ²}
   │
 [반증]  반례를 찾아 깨거나 좁힌다  예: d_min 반례, "comb magic 0 ⟺ 주기 2^k"의 *거의*-참
   │
 [승격]  살아남으면 증명·명제화      예: 명제 4(가법에너지), 명제 5(Sidon)
```

이 루프의 **2·5단계는 이미 기계가 한다**(`sre2`가 측정, 사람이 증명). **공백은 1·3·4단계의 자동화**다:

- **1 생성**: 지금은 사람이 "마커집합을 랜덤/Sidon/아핀으로 만들자"고 결정.
- **3 추측**: 지금은 사람이 데이터를 보고 `log₂(M³/(7M−6))`를 *눈으로* 맞춘다(symbolic regression의 수작업판).
- **4 반증**: 지금은 사람이 `{0,1,2,3}` vs `{0,1,2,4}` 같은 반례를 *직관으로* 찾는다.

> **따라서 "수학적 직관을 코딩한다"는 것은 추상적 목표가 아니라, 이 세 단계를 명시적 탐색 알고리즘으로
> 바꾸는 구체적 엔지니어링 과제다.**

---

## 2. "AI 시스템"의 두 해석 — 분리해서 검토

사용자 요청의 "수학적 직관을 직접 코딩 / 알고리즘을 찾아내는"은 사실 **두 개의 다른 목표**이며, 난이도가 다르다.

| | (A) 직관을 코딩 = **법칙/정리 발견** | (B) 알고리즘을 발견 = **절차 최적화** |
|---|---|---|
| 찾는 대상 | 불변량 사이의 *닫힌 관계식* | 더 빠른/적은 자원의 *계산 절차* |
| 이 레포 예 | 명제 4·5(가법에너지·Sidon), σ-곡선 | (C) 후처리, b-trick 하이브리드, T-count 절감 |
| 탐색 공간 | 수식 트리(연산자·상수) | 프로그램/회로 트리(게이트·후처리 단계) |
| 검증 | 수치 일치 + 반례 부재 + 증명 | 정확성(불변) + 비용 측정(K, T-count) |
| 성숙한 기법 | **symbolic regression, sparse dictionary** | **program/circuit synthesis, RL, superoptimization** |
| 본 레포 적합도 | **높음(즉시 PoC 가능)** | 중간(보상함수가 이미 정의됨: K, T-count) |

**검토 결론**: (A)가 진입점으로 압도적으로 유리하다 — 측정이 정확(10⁻¹⁵)하고, 정답(이미 손으로 찾은 닫힌형)이
있어 *재발견 벤치마크*가 가능하며, 탐색공간이 작다. (B)는 (A)가 검증된 뒤 같은 인프라 위에 얹는다.

---

## 3. 아키텍처 — JAMES-DISCOVER 파이프라인

다섯 모듈. 전부 기존 numpy-only 인프라 위에 얹히고, 외부 네트워크 불필요.

```
┌──────────┐   ┌─────────┐   ┌──────────┐   ┌───────────┐   ┌──────────┐
│ Generator│──▶│  Probe  │──▶│  Miner   │──▶│ Adversary │──▶│ Promoter │
│ 구조 생성 │   │ 불변량   │   │ 추측 합성 │   │ 반례 탐색  │   │ 증명 승격 │
└──────────┘   └─────────┘   └──────────┘   └───────────┘   └──────────┘
     ▲                                              │
     └──────────── 반례를 새 생성 시드로 되먹임 ──────┘
```

### 3.1 Generator — 구조 샘플러
- 출력: 라벨된 객체. 예) 마커집합 `W`(랜덤/Sidon/아핀/부호기반), 불리언함수 `f`(ANF 차수별),
  Grover 다중표시 집합, 잡음 파라미터.
- 핵심 설계점: **분포 다양성**. 발견 품질은 생성 분포가 관계식의 변수를 충분히 흔드는지에 달림
  (예: M₂=f(M)을 찾으려면 M을 넓게, A_W를 찾으려면 비-Sidon W도 포함).
- 기존 자산 재사용: `marker_code_closed_form.py`의 `subspace`, `greedy_sidon`, `flat`.

### 3.2 Probe — 불변량 측정기 (이미 있음)
- 입력 객체 → 특징벡터. `magic.py:sre2`(M₂), 자기상관 `A_W(x)=|W∩(W⊕x)|`, `τ(W)`,
  가법에너지 `E(S)`, ANF 단항식 수 `T_proxy`, σ-곡선 K 등.
- **신뢰 근거**: 3중 교차검증(XOR-FWHT↔Pauli순열↔kron) 10⁻¹⁵, 회귀 42 assert. 측정층은 그대로 신뢰.

### 3.3 Miner — 추측 합성기 (핵심 신규)
- 두 갈래를 병행:
  1. **Sparse dictionary fit (선검토 권장)**: 후보 기저함수 집합
     `{log₂M, log₂N, M²/N, log₂Σ_x A_W(x)², τ, …}`에 대해 L1/직교매칭추구로 *희소* 선형결합을 찾음.
     장점: 빠르고 해석가능, 과적합 통제 쉬움. 명제 4·5는 사실상 이 형태(`-log₂((1/M⁴)ΣE)`).
  2. **Symbolic regression (확장)**: 연산자 트리 `{+,−,×,÷,log,exp,pow}` 위 유전/빔 탐색.
     장점: 미지 형태도 발견(예: `τ^{0.32}` 같은 비정수 지수 추세). 단점: 과적합·거짓발견 위험 큼.
- 출력: 후보 관계식 + 잔차(R², max|err|) + 복잡도(연산자 수).
- **선택 기준 = 정확도 × 단순성** (MDL/파레토). 이 레포의 미학("닫힌형 4e-15, 단순")과 정렬됨.

### 3.4 Adversary — 반례·범위 탐색기 (자기수정의 기계화)
- **이 프로젝트의 정체성이 여기 있다.** 9+건의 철회는 전부 "더 넓게 측정하니 깨졌다"의 결과.
  AI 시스템은 *반드시* 능동적 반증기를 내장해야 한다(없으면 과대주장 양산기).
- 동작: Miner가 낸 식에 대해 **반례를 적극적으로 찾는** 탐색(범위 확장, 경계 케이스, 적대적 W).
  - 깨지면: "성립 범위"를 좁히고(`comb magic 0 ⟺ 주기 2^k`가 *거의*-참이 된 것처럼) 재제출.
  - 안 깨지면: 신뢰 점수↑, Promoter로.
- 부가: **민감도/단조성 체크**(Theorem 6식 "부호 불문 단조" 같은 구조적 사실 자동 후보화).

### 3.5 Promoter — 증명 후보 승격기
- 살아남은 식을 **증명 골격**과 함께 제시: 알려진 항등식 매칭(Parseval, Walsh 4차모멘트),
  특수값 환원 체크(M=1→0, 아핀→0), 점근 일치(M→∞).
- **여기까지가 AI의 자동 범위**; 형식증명은 사람 또는 (선택) 증명보조기/LLM. 자동 생성식을
  명제로 *주장*하지 않고 *후보*로만 큐에 넣는 것이 신규성 환각·과대주장 방지의 핵심.

---

## 4. 왜 이 도메인이 자동 발견에 유난히 잘 맞나

일반적으로 "AI 수학 발견"은 과대선전되지만, **이 레포는 세 가지 희귀 조건을 동시에 만족**한다:

1. **측정이 정확하고 싸다.** `sre2`는 닫힌형 항등식으로 *근사 아닌 정확*(10⁻¹⁵). 발견 루프의
   신호잡음비가 매우 높다 — 잡음 많은 실험 데이터에서 법칙 찾는 일반 과학 문제와 질적으로 다름.
2. **닫힌형이 실제로 존재한다.** 명제 4·5가 증거. "찾을 게 있는" 도메인. (모든 도메인이 이렇지 않다.)
3. **상태공간이 작다(검증가능).** `O(n·4ⁿ)`로 `n≲14–16`이지만, 닫힌형은 작은 n에서 발견하고
   해석적으로 일반화한다(이미 하는 방식). 작은 n = 반례 탐색이 *전수에 가깝게* 가능.

추가로, **재발견 벤치마크**가 공짜로 있다: AI가 Sidon 값·τ 영점판정·가법에너지 닫힌형을 *스스로*
찾아내는지로 시스템 유효성을 객관 평가할 수 있다(아래 6절).

---

## 5. 첫 정착지(Generator 후보) — 무엇을 발견시킬까

`magic-program-overview.md` Part II의 *미완/잔여* 항목이 그대로 AI의 표적이 된다:

| 표적 | 현재 상태 | AI에게 줄 입력 | 기대 산출 |
|---|---|---|---|
| **Δ(M,N) 유한-N 보정항** | 과제 A 잔여(경험적 단조) | (M,N,E[M₂]) 표 | Sidon값−E[M₂]의 닫힌 보정식 |
| **비-flat Grover 닫힌형** | 과제 B 잔여(분해는 가능) | 2-진폭 상태 magic 궤적 | M(반복수) 함수의 명시식 |
| **τ↔M₂ 정확 관계** | 추세 `M₂≈7.7τ^{0.32}` (r=0.76) | (τ, M₂) 산점 | 더 나은 닫힌형 또는 다변수식 |
| **autocorrelation 예측자** | `b0b4948`에서 시작 | A_W 스펙트럼 | M₂의 A_W-기반 예측식 |

이들은 전부 **현재 미해결**이고 **검증가능**해서, AI 시스템의 첫 실전 가치를 즉시 측정할 수 있다.

---

## 6. 실현가능성 — 단계별, 그리고 "성공"의 정의

PoC의 성패는 모호한 인상이 아니라 **재발견 통과/실패**로 판정한다:

- **D1 (PoC) — 재발견 게이트. ✅ 구현·통과 (`experiments/discover_poc.py`).**
  Generator(마커집합) + Probe(기존) + Miner(최소부분집합 정확탐색) + Adversary(범위축소) + Promoter(정수스냅).
  **결과**:
  - **[T1 PASS]** 사람 힌트 없이 Sidon 마커상태의 ξ를 사전 `{M⁻¹,M⁻²,M⁻³,M⁻⁴,log₂M·M⁻²}`에서
    최소부분집합으로 적합 → **정확히 `ξ = 7·M⁻² − 6·M⁻³`** 복원(잔차 4.7e-16, 상수 7·−6 정수 스냅)
    ⟹ `M₂ = log₂(M³/(7M−6))` 자동 재발견(= 명제 5). 점근 `2log₂M−log₂7`도 자동 확인.
  - **[T2 PASS]** 후보 영점-예측자 `{τ, mean A_W, max A_W, d_min}` 중 `f=0 ⟺ M₂=0`을 **위반 0**으로
    만족하는 것을 선택 → **τ** 채택(보조정리 1 재발견), 나머지 18위반으로 탈락. `{0,1,2,3}`(M₂=0) vs
    `{0,1,2,4}`(M₂=1.54)의 동일 d_min 반례를 자동 박제(인수인계서 d_min 지표 정정 재확인).
  - **[Adversary]** 복원식을 랜덤 W로 적대 시험 → `M²/N ≳ 0.25`에서 식이 깨짐을 스스로 보고하고
    "성립범위 = Sidon 영역(M²≪N)"으로 자동 축소(= 명제 5의 유한-N 가법충돌 보정 재확인).
  - **시사**: (A) 법칙발견 경로가 이 도메인에서 실현가능함을 *객관 수치로* 확인. 다음은 D3(미탐 표적).
- **D2 — Adversary 장착.** D1 식에 반례탐색을 붙여 "성립 범위" 자동 축소가 d_min 반례 수준으로
  동작하는지. 합격: 알려진 *거의*-참 명제의 예외를 스스로 발견.
- **D3 — 미탐 표적(5절) 투입.** Δ(M,N) 또는 비-flat Grover에서 *새* 후보식 산출. 합격: 사람이
  검증 가능한 신규 닫힌형 1개 제시(증명은 별개).
- **D4 (확장, 선택) — (B) 알고리즘 발견.** 보상=K 또는 T-count로 후처리/회로 절차 superoptimization.
  기존 `oracle_tcount_magic.py`·`method_bakeoff.py`가 보상 측정 인프라를 이미 제공.

**비용 현실성**: D1–D3는 전부 로컬 numpy, 외부 네트워크 불필요(현 환경 egress 차단과 무관).
D1은 기존 `marker_code_closed_form.py`의 생성·측정 함수 재사용으로 신규 코드가 작다.

---

## 7. 위험과 완화 — 이 팀이 가장 잘 아는 함정

| 위험 | 왜 이 프로젝트에서 특히 중요한가 | 완화 |
|---|---|---|
| **거짓발견/과적합** | 9+건 철회의 직접 원인이 "성급한 일반화" | Adversary 필수 내장; MDL/파레토; train/test n 분리; 재발견 게이트로 보정 |
| **신규성 환각** | 명제 4가 사실 2311.08463 선점이었음(전문확인 후 credit) | 자동식은 *후보*로만; Promoter가 항등식 매칭만, 신규성 주장 금지; 선행조사는 사람 |
| **n 한계(O(n·4ⁿ))** | 큰 n 거동 못 봄 | 작은 n 발견 → 해석적 점근(이미 하는 방식); n-외삽은 검증대상으로 명시 |
| **상수 환각(spurious π, 7 등)** | `7M−6`의 7은 의미있지만 우연 상수도 잘 나옴 | 상수는 유리수/소정수로 제약; 특수값 환원(M=1→0)으로 필터 |
| **측정층 과신** | sre2는 검증됐지만 새 Probe(τ 등)는 아닐 수 있음 | 새 불변량마다 brute-force 교차검증(`sre2_bruteforce` 패턴) 의무화 |
| **egress 차단** | 과제 E(선행 전문대조)는 네트워크 필요 | D1–D3는 로컬만; 선행대조는 발견과 분리해 사람이 수행 |

> **핵심 원칙**: AI 시스템은 *주장기*가 아니라 *후보 생성기*다. 이 프로젝트의 신뢰는 "정직한
> 자기수정"에서 왔다(README 끝맺음과 동일). 자동화가 그 미덕을 훼손하면 안 되므로, **Adversary와
> "후보일 뿐" 규율을 1급 설계 제약으로 못박는다.**

---

## 8. LLM/생성형 AI 통합 — 선택지 (필수 아님)

핵심 발견 루프는 LLM 없이 동작한다(symbolic regression은 기호 탐색). LLM은 *보조*로만 가치:

- **가설 시드 제안**: "어떤 기저함수를 사전에 넣을까"를 LLM이 제안(사람 검토). 탐색 가속용.
- **증명 골격 초안**: Promoter 후보에 대해 알려진 항등식 연결 초안(반드시 사람 검증).
- **선행조사 요약**: 네트워크 허용 시 arXiv 대조(과제 E) 보조 — 단, 신규성 *판정*은 사람.

권장: **D1–D3는 LLM 없이**(결정성·재현성 우선). LLM은 D3 이후 "탐색공간 제안" 역할로만 도입 검토.

---

## 9. 권장 결정 사항

1. **(A) 법칙발견 먼저, (B) 알고리즘발견 나중** — 동의 여부.
2. **D1 재발견 게이트를 첫 산출물로** — Sidon·τ 자동복원을 합격선으로 둘지.
3. **Miner 1순위: sparse dictionary**(해석가능·과적합 통제) vs symbolic regression(표현력) 중 시작점.
4. **LLM 통합은 D3 이후로 보류** — 동의 여부.

위 결정이 서면 D1 PoC 설계(파일 구성·재발견 벤치마크·합격 기준 수치)를 별도 문서/코드로 진행한다.

---

## 부록 — 기존 자산 매핑 (재사용 가능, 신규 최소화)

| 파이프라인 모듈 | 재사용할 기존 자산 |
|---|---|
| Generator | `marker_code_closed_form.py`: `flat`, `subspace`, `greedy_sidon` |
| Probe | `magic.py`: `sre2`, `sre2_bruteforce`; `marker_code_magic.py`: `A_W`, `τ`; `additive_energy` |
| Miner | (신규) sparse dict / symbolic regression — numpy 선형대수 + 작은 탐색기 |
| Adversary | (신규) 범위확장 샘플러 + 반례기록 — 기존 생성기 재호출 |
| Promoter | `magic_proofs_check.py`(42 assert) 패턴 = 특수값/점근 자동 체크의 틀 |
| 보상(B용) | `oracle_tcount_magic.py`(T-count), `method_bakeoff.py`(K 비교) |
